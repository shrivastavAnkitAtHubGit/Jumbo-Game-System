const UserModel = require('./user.model');
const GameModel = require('./game.model');
const QuestionModel = require('./question.model');
const SessionModel = require('./session.model');
const UseSessionStatModel = require('./userSessionStat.model');
const UserSessionSubmission = require('./userSessionSubmission.model');

module.exports = {
    UserModel,
    GameModel,
    QuestionModel,
    SessionModel,
    UseSessionStatModel,
    UserSessionSubmission,
};